#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int maxn = 500 + 5;
char a[maxn][maxn];

bool dfs(int st) {
	i
}
int main()
{
	ios::sync_with_stdio(false); cin.tie(0);

	int t; cin >> t;
	while (t--) {
		int n, m; cin >> n >> m;
		for (int i = 0; i < n; ++i)
			cin >> a[i][j];
	}
}